{
  "name": "@samples/oauth2-client",
  "version": "1.0.0",
  "private": true,
  "description": "Sample application to verify NetSuite OAuth 2.0 client authentication",
  "type": "module",
  "scripts": {
    "start": "tsx --env-file=.env index.ts"
  },
  "dependencies": {
    "@kitledger/netsuite-auth": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
